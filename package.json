{
  "name": "zerith",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:player": "npm run dev --workspace=player",
    "dev:editor": "npm run tauri dev --workspace=editor"
  }
}