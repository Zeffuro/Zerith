export {
    fsDirname,
    fsJoin,
    fsMkdir,
    fsOpenPath,
    fsReadDir,
    fsReadTextFile,
    fsRemove,
    fsRename,
    fsWriteTextFile,
} from './explorerFs';

export type { FsDirEntry } from './explorerFs';

