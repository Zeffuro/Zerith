export {
    fsDirname,
    fsJoin,
    fsMkdir,
    fsOpenPath,
    fsReadBinaryFile,
    fsReadDirectory,
    fsReadTextFile,
    fsRemove,
    fsRename,
    fsWriteBinaryFile,
    fsWriteTextFile,
} from './explorerFs';

export type { FsDirectoryEntry } from './explorerFs';

