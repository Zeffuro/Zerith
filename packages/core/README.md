# @zeffuro/zerith-core

Runtime engine, schemas, and content utilities for Zerith visual novel projects.

```ts
import { parseSceneFile } from '@zeffuro/zerith-core/schemas';
import { analyzeStoryGraph } from '@zeffuro/zerith-core/utils/StoryGraph';
```

The desktop editor is released separately as GitHub installer artifacts.
